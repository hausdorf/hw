var = b1 * b0 * a1 * a0
# Case 1: the rightmost digits overflow on their own
# Case 2: the rightmost digits overflow when added AND
# at least ONE of the leftmost digits is a 1
f = ((b1==1)*(a1==1)) + ( ((b0==1)*(a0==1)) * ((b1==1)+(a1==1)) )
[f]
